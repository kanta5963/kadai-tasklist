<h1>user<%= @tasklist.id %>編集ページ</h1>

<%= form_for(@tasklist) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '戻る', tasklist_path %>