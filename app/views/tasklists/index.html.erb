<h1>タスクリスト一覧</h1>

<ul>
  <% @tasklist.each do |tasklist| %>
  <li><%= link_to tasklist.id, tasklist %> :<%= tasklist.status %> > <%= tasklist.content %></li>
  <% end %>
</ul>

<%= link_to '新規投稿', new_tasklist_path %>